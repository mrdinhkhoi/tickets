<%= form_for(@ticket_attachment) do |f| %>
  <% if @ticket_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_attachment.errors.count, "error") %> prohibited this ticket_attachment from being saved:</h2>

      <ul>
      <% @ticket_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ticket_id %><br>
    <%= f.number_field :ticket_id %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
